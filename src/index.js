//imports the various React modules
import React from 'react';
import ReactDOM from 'react-dom';

//imports the component for the app
import App from './App';

//updates the React virtural DOM and webpage
ReactDOM.render(<App />, document.getElementById('root'));